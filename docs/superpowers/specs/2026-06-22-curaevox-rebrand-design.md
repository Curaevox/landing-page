# Curaevox — Homepage Rebrand Design

**Date:** 2026-06-22
**Status:** Approved (build)

## Product
Curaevox builds AI voice agents for patients and doctors that solve the healthcare **inbound** problem and the **outbound follow-up** gap.

- **Inbound agent:** patient calls a number → agent converses naturally → fetches doctor details, specialties & availability (tools) → captures issue/info needed → recommends requirements & doctor → books the slot automatically. Surfaces to the doctor dashboard.
- **Outbound follow-up agent:** calls patients after an appointment → produces structured follow-up reports tied to the patient, delivered to the doctor dashboard.
- **Doctor dashboard / AI companion:** every patient has an agent (the doctor's AI companion). Doctors configure agent actions, monitor, attach, note, save, and summarize conversations per patient.

Name = **Cura** (care) + **vox** (voice): "Care, given a voice."

## Audience & positioning
Primary buyer: **hospitals & clinics (B2B)**. Users: patients + doctors. Lead with ROI (captured calls, fewer no-shows, doctor time saved) and a unique **equity** angle (consistent, multilingual, bias-free intake). Market framing: global / market-agnostic.

## Tagline
**Primary:** "Care that always picks up."

## Brand system
- **Palette:** ink `#06312F`, brand teal `#0E6B6F`, coral signal accent `#FF6A4D` (voice/live motif), mist `#CFE6E3`, halo `#EAF3F1`, canvas `#F6F8F7`, line `#E2E8E6`, surface `#FFFFFF`. Muted text `#41534F`.
- **Type:** Bricolage Grotesque (display) + Inter (UI/body), via `next/font/google`. Removes the Faire Octave serif entirely.
- **Logo:** voice-pulse resolving into a care "+", teal with coral pulse dot, "Curaevox" wordmark.

## Homepage narrative (sections → existing components)
1. Hero — tagline, "the phone is the front door, often unanswered", demo CTA, live-call agent visual w/ coral waveform. (`Hero`)
2. Problem-in-numbers stat bar. (`Investors` → trust/stat bar)
3. Problem → solution crossfade. (`ProblemSolution`)
4. How it works — inbound + outbound flow, horizontal scroll: Answer & triage → Match doctor → Book slot → Follow up. (`ProductSections`)
5. Product pillars — Inbound Agent · Outbound Follow-up · Doctor Dashboard · per-patient AI companion. (`WhyEase` / `ClinicalSupport`)
6. Built for equity — peer-reviewed audit data. (`Stats` or new block)
7. The ratio problem — WHO ~10M shortfall by 2030; AAMC ≤86k US shortage by 2036. (`Stats`)
8. Trust/compliance (HIPAA-ready) → case/testimonial placeholder → CTA banner → footer. (`Compliance`, `CaseStudy`, `CtaBanner`, `Footer`)

## Citable stats (from research)
- Outpatient no-show **23–33%** (industry aggregation, 2024-25).
- ~⅓ inbound calls missed; most callers don't retry (vendor — present as range).
- New-patient wait **~26 days** avg, 15 US metros (AMN/Merritt Hawkins 2022).
- **36.8%** discharged patients had no follow-up scheduled; readmission 8.8% vs 6.0% (J Gen Intern Med 2021).
- Bias: Black callers +44pp insurance gatekeeping; longer waits by race/language (JAMA Network Open 2020; Health Affairs Scholar 2023).
- Shortage: WHO ~**10M** health-worker shortfall by 2030; AAMC up to **86,000** US physician shortage by 2036; ~2 hrs EHR/desk per 1 hr patient care (Sinsky, Ann Intern Med 2016).

## Scope (this pass)
Full homepage end-to-end + Header/Footer/Logo/nav/fonts/colors/metadata. Restub Product/Solutions pages with on-brand content. Subpages (blog, glossary, etc.) get brand shell but not full rewrite.

## Non-goals
No backend/auth/real dashboard. No real customer logos/testimonials (clearly placeholder). No pixel-cloning any competitor.
